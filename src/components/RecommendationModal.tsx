import React from 'react';

interface RecommendationModalProps {
  isOpen: boolean;
  onClose: () => void;
}

const RecommendationModal: React.FC<RecommendationModalProps> = () => {
  // Componente vaciado deliberadamente.
  return null;
};

export default RecommendationModal;